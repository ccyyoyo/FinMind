class SocialDataSource:
    """Placeholder for social sentiment (Reddit/Twitter/etc.)."""

    def fetch(self, symbol: str, window_days: int) -> dict:
        _ = (symbol, window_days)
        return {"posts": []}

